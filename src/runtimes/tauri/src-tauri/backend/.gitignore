# This directory is generated by the beforeBuildCommand in tauri.conf.json.
# It copies the Python backend sources into the proper package structure
# so Tauri can bundle them as resources with correct import paths.
# The PLACEHOLDER file is created by build.rs for cargo test/check.
*
!.gitignore
