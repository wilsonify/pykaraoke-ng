"""Compatibility test suite: Python ↔ Rust engine comparison.

Each test module exercises the same logic through both the Python
reference implementation and the native Rust engine, then compares
outputs to detect regressions.
"""
